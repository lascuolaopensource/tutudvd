# tutudvd
festa di tutu projection

## Live reload (dev)

```bash
npm install
npm run dev
```

This starts a local server with automatic browser refresh on file changes:
- URL: `http://127.0.0.1:8080`
- Watched files: project root (`index.html`, `app.js`, `style.css`, etc.)
